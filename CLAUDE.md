# REPO_avatarAI

This repository holds the complete AvatarAI application: the upstream project by Punith V T, released under the MIT License, plus the changes made here.

- **Keep the attribution.** `LICENSE` is Punith V T's MIT License and stays as it is, in every copy. Credit him as the original author, and never imply that he made or endorses this version.
- **Upstream updates** come from the `origin` remote (`PunithVT/ai-avatar-system`). This repository is not a GitHub fork of it.
- **Do not split the app out of this repository.** It is kept whole on purpose: the changes here are edits inside the app's own files, not a separate add-on, and 22 of the upstream files are modified.
- **Commit style** follows `CONTRIBUTING.md`: Conventional Commits.

## Notes kept outside this repository

`DECISIONS.md` records the decisions behind this setup and is deliberately not tracked by git. It is listed in `.gitignore` and stays on the local machine.
